# Dmytro Poliakov — Junior DevOps portfolio

Static portfolio with green light and near-black dark themes, a saved theme preference, project filters, workflow views, and illustrative demos.

## Preview

Run `python3 -m http.server 8080` in this folder and open http://localhost:8080.

## GitHub Pages

Upload every file in this folder to the repository root. Keep `index.html`, `styles.css`, `theme.css`, `app.js`, and `favicon.svg` together. Include `.nojekyll`. Select the publishing branch and root folder in repository Settings → Pages. Asset paths support the repository subpath.

The archive is ready to upload; it does not mean a site has been published. The target repository is `sirDmitryj/devops-portfolio` and no working deployed URL was verified during this update.

## Honest project scope

The Docker card links to the implemented Flask and Docker project at https://github.com/sirDmitryj/devops-docker-ci-demo. Its GitHub Actions workflow builds the Docker image. Local health validation is described separately. Linux practice and the document assistant prototype reflect the supplied CV. No professional DevOps role is claimed. All interactive demos are browser simulations using sample data.

The Linux repository placeholder can be changed in `app.js`, followed by the matching card and evidence copy. No full CV, private hosting metadata, credentials, or analytics are included. The noindex metadata is not access control.

## Checked

Both themes at 320, 390, 700, 768, 1024, and 1440 pixels. Theme persistence, system defaults, filters, project dialogs, demo scenarios, keyboard focus, and relative assets were verified in Chromium.
